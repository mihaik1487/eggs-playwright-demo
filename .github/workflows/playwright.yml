name: Run Playwright Tests

on:
  pull_request:  # Trigger on PRs only

jobs:
  smoke-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Smoke Tests
        run: |
          npx playwright test --grep @smoke --reporter=html
        continue-on-error: true  # Allow report upload even if tests fail

      - name: Upload HTML Report
        if: always()  # Always upload report, even if tests fail
        uses: actions/upload-artifact@v4
        with:
          name: playwright-smoke-report
          path: playwright-report


  reggression-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Smoke Tests
        run: |
          npx playwright test --grep @regression --reporter=html
        continue-on-error: true  # Allow report upload even if tests fail

      - name: Upload HTML Report
        if: always()  # Always upload report, even if tests fail
        uses: actions/upload-artifact@v4
        with:
          name: playwright-smoke-report
          path: playwright-report